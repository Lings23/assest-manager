# 快速开始指南

## 方式一:使用自动化脚本(推荐)

### Windows系统

1. **双击运行** `setup-and-run.bat`
2. 脚本会自动:
   - 检测并安装Go环境
   - 下载项目依赖
   - 编译项目
   - 启动服务
3. 浏览器访问 http://localhost:8080
4. 使用默认账号登录: admin / admin123

### Linux系统

```bash
# 添加执行权限
chmod +x setup-and-run.sh

# 运行脚本
./setup-and-run.sh
```

## 方式二:手动安装

### 第1步:安装Go

#### Windows
```powershell
# 方法1:使用winget
winget install GoLang.Go

# 方法2:下载安装包
# 访问 https://golang.org/dl/ 下载并安装
```

#### Linux
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install golang-go

# CentOS/RHEL
sudo yum install golang

# 或使用snap
sudo snap install go --classic
```

验证安装:
```bash
go version
# 应显示: go version go1.22.x ...
```

### 第2步:进入项目目录

```bash
cd "c:\D盘\工作文档\Lingma文件夹\workspace"
```

### 第3步:下载依赖

```bash
go mod tidy
```

如果网络较慢,可以配置代理:
```bash
go env -w GOPROXY=https://goproxy.cn,direct
```

### 第4步:运行项目

```bash
# 直接运行
go run main.go

# 或先编译再运行
go build -o asset-manager.exe
./asset-manager.exe
```

### 第5步:访问系统

浏览器打开: http://localhost:8080

默认管理员账号:
- 用户名: admin
- 密码: admin123

## 常见问题

### Q1: 提示"go: command not found"

**原因**: Go未安装或未添加到PATH

**解决**:
1. 安装Go (见第1步)
2. 重新打开命令行窗口
3. 验证: `go version`

### Q2: 依赖下载失败

**原因**: 网络连接问题

**解决**:
```bash
# 配置国内代理
go env -w GOPROXY=https://goproxy.cn,direct

# 重新下载
go mod tidy
```

### Q3: 端口8080被占用

**解决**:
```bash
# Windows设置环境变量
set PORT=8081
go run main.go

# Linux设置环境变量
export PORT=8081
go run main.go
```

### Q4: 编译错误

**可能原因**:
1. Go版本过低 (需要1.22+)
2. 依赖不完整

**解决**:
```bash
# 检查Go版本
go version

# 重新下载依赖
go clean -modcache
go mod tidy

# 重新编译
go build
```

### Q5: 数据库文件在哪里?

数据库文件自动创建在:
```
workspace/data/assets.db
```

### Q6: 如何重置管理员密码?

删除数据库文件,重启系统:
```bash
# Windows
del data\assets.db
go run main.go

# Linux
rm data/assets.db
go run main.go
```

系统会自动创建默认管理员账号。

## 开发流程

### 修改代码后重新运行

```bash
# 停止正在运行的程序 (Ctrl+C)

# 重新编译
go build -o asset-manager.exe

# 运行
./asset-manager.exe
```

### 查看日志

程序会在控制台输出运行日志,包括:
- 数据库初始化信息
- HTTP请求日志
- 错误信息

## 下一步

系统启动成功后:

1. **登录系统** - 使用 admin/admin123
2. **查看需求文档** - 了解系统功能
3. **参考开发指南** - 继续开发未完成的功能
4. **查看项目状态** - 了解当前进度

## 技术支持

如遇到问题:

1. 查看 README.md
2. 查看 开发指南.md
3. 查看 项目状态.md
4. 检查控制台错误日志

---

**祝您使用愉快!**
