# 工程化改造状态

## 当前结论

- 记录日期：2026-07-22
- 当前阶段：阶段一（第 1–2 月）——旧系统止血与工程基线
- 阶段状态：已完成——实现与 Compose 全栈运行验收均已通过
- 下一阶段：阶段二（第 3–4 月）——IAM 与元数据资产引擎（未开始）
- 执行约束：阶段一退出条件通过后停止，不提前实现 IAM、资产 Schema 或阶段二业务。

## 本阶段完成内容

| 范畴 | 已完成改造 | 主要证据 |
|---|---|---|
| 字段与 SQL 安全 | 七类资产创建、更新只接受服务端字段配置中的业务字段；拒绝未知字段及 `id/created_by/created_at/updated_at/is_deleted`；更新时把本人数据范围放入同一条 SQL 条件 | `internal/handlers/assets_all.go`、`internal/handlers/security_regression_test.go` |
| 持久化 XSS | 新增统一 HTML 转义工具；资产表格、页面标题、用户名、导入结果和错误信息按文本展示；补充 CSP、禁止嵌入和 MIME 嗅探等响应头 | `web/js/utils/html.js`、`web/js/assets/utils/table-renderer.js`、`internal/routes/routes.go` |
| Token 存储 | Access Token 仅保存在页面内存，不再使用 `localStorage` 或 `sessionStorage`；刷新页面后要求重新登录 | `web/js/api.js`、`web/js/auth.js` |
| JWT 与注销 | JWT 密钥从部署环境读取，缺失或过短时生成进程级随机密钥；限制为 HS256、增加 issuer/JTI/到期时间；注销和改密后撤销当前 Token | `internal/config/config.go`、`internal/utils/jwt.go`、`internal/handlers/auth.go` |
| 管理员与密码 | 移除固定 `admin/admin123` 初始化；支持部署密钥 `ADMIN_PASSWORD`，否则生成并仅在首次启动日志中输出随机密码；发现旧默认密码哈希时自动轮换；新增本人改密接口 | `internal/database/database.go`、`internal/handlers/auth.go`、`internal/routes/routes.go` |
| 登录防护 | 以客户端 IP 和用户名组合实施失败窗口与临时锁定；未知用户执行等成本密码校验；禁用默认可信代理，避免伪造转发头绕过限流 | `internal/handlers/auth.go`、`internal/routes/routes.go` |
| 统计正确性 | 高风险等级按数据库整数编码 `0` 统计；管理员与填报员使用统一数据范围；处理 Query、Scan 和 `rows.Err()`，数据库故障不再静默返回伪结果 | `internal/handlers/stats.go`、`internal/handlers/security_regression_test.go` |
| 条件与类型校验 | 布尔值在条件规则中统一识别；外部接口为真时强制填写对接范围；枚举、数字和日期统一进行服务端校验；响应布尔值保持 JSON boolean | `internal/handlers/validation.go`、`internal/handlers/assets_all.go` |
| 旧导入止血 | 请求体限制 50 MiB；限制扩展名与 MIME；逐行复用业务校验；非法布尔、枚举、数字和日期拒绝；返回明确的部分成功标记 | `internal/handlers/import.go` |
| RBAC 统一 | 明确 admin/reporter 在列表、详情、新建、更新、删除、导入、导出、统计和累计计算上的数据范围；前端仅管理员显示删除操作 | `权限矩阵.md`、`web/js/assets/utils/table-renderer.js` |
| SQLite 稳定性 | 使用单写连接，启用 WAL、外键和 busy timeout，并检查初始化错误 | `internal/database/database.go` |
| 路线可执行性 | 在总计划中逐项标注第 1–12 月主题、工作和退出条件 | `工程化改造计划.md` |

## 第 2 月完成内容

| 范畴 | 已完成改造 | 主要证据 |
|---|---|---|
| Monorepo | 根模块保留冻结旧系统；新增 Go workspace、`platform` 模块、`apps/web`、`api`、`deploy`、`scripts` 和架构文档 | `go.work`、`docs/architecture/README.md` |
| 五个 Go 进程 | 建立 gateway、iam-service、asset-service、governance-service、task-report-service，可分别构建和启动 | `platform/cmd/`、`platform/Dockerfile` |
| 服务工程基线 | 统一配置校验、JSON 结构化日志、请求 ID、错误信封、安全响应头、CORS 白名单、请求体上限、健康检查、HTTP 超时、panic 恢复和优雅停机 | `platform/internal/servicekit/` |
| API Gateway | 按 auth、assets/asset-types、workflows/risks、import/export/reporting 路径转发到四个领域服务；上游故障返回标准 502 错误 | `platform/cmd/gateway/` |
| Vue 3 前端 | 建立 Vue 3、TypeScript、Vite、Pinia、Vue Router、Element Plus 工程；Access Token 只进入 Pinia 内存；Nginx 提供严格脚本 CSP 与网关反代 | `apps/web/` |
| OpenAPI 与生成 | 建立 `/api/v1` 公共契约；从契约生成 Go 路由清单和 SHA-256；CI 检查生成漂移 | `api/openapi/v1.yaml`、`tools/contractgen/`、`platform/internal/contract/routes_gen.go` |
| Compose | 编排 PostgreSQL、RabbitMQ、MinIO、五个 Go 进程和 Vue 前端；四个领域使用独立 PostgreSQL Schema；密码必须显式配置；基础设施与构建镜像均固定到已验证的多架构摘要 | `compose.yaml`、`platform/Dockerfile`、`apps/web/Dockerfile`、`deploy/postgres/init/001-schemas.sql`、`.env.example` |
| CI/CD 基线 | 建立格式、单测、竞态、vet、Vue 类型/构建、OpenAPI lint、生成一致性、Compose 冒烟、govulncheck、npm audit、Trivy 密钥/配置扫描和 Dependabot | `.github/workflows/ci.yml`、`.github/dependabot.yml` |
| 功能冻结 | 文档化旧系统允许和禁止的变更；基线合并后 CI 阻止未批准的旧系统功能修改 | `docs/legacy-freeze.md`、`.github/workflows/ci.yml` |
| 依赖安全 | 升级 JWT、`x/net`、`x/crypto` 等安全版本；升级 Vitest 并重新锁定，清除可达 Go 漏洞和 npm 漏洞 | `go.mod`、`go.sum`、`apps/web/package-lock.json` |

## 验收结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| Go 单元与回归测试 | 通过 | `go test -count=1 ./...`；handlers 与 utils 测试通过 |
| Go 静态检查 | 通过 | `go vet ./...` 无输出、退出码 0 |
| 可执行文件构建 | 通过 | `go build` 成功生成临时 Windows 构建产物 |
| 前端模块语法 | 通过 | Node 对 25 个 JavaScript 模块逐一执行语法检查 |
| 浏览器持久化存储扫描 | 通过 | `web` 内未发现 `localStorage` 或 `sessionStorage` 引用 |
| XSS 转义样本 | 通过 | `<img src=x onerror=alert(1)>` 输出为转义文本，不生成 HTML 节点 |
| 真实 HTTP 冒烟 | 通过 | 管理员登录成功；提交系统字段返回 400；改密与注销后旧 Token 均返回 401；响应包含 CSP |
| 新平台 Go 测试 | 通过 | gateway 路由、配置、健康检查、请求 ID、标准错误和 CORS 白名单测试通过 |
| 五进程构建与跨进程冒烟 | 通过 | 五个 Windows 进程均成功构建和启动；5 个健康检查成功；网关转发与错误信封、CORS 204/403 通过 |
| Vue 类型检查与生产构建 | 通过 | `npm run typecheck` 和 `npm run build` 成功，1610 个模块完成构建 |
| OpenAPI | 通过 | Redocly 判定契约有效；生成产物 SHA 与路由清单一致 |
| 依赖安全 | 通过 | `npm audit` 为 0 vulnerabilities；旧系统和新平台 `govulncheck` 均为 0 个可达漏洞 |
| Compose 语义配置 | 通过 | 下载 Docker 官方 Compose v5.1.4 Windows 独立程序并按官方 `checksums.txt` 校验 SHA-256（`e1a8faff…f39aa`）；`compose config --quiet` 通过 |
| 容器镜像引用 | 通过 | Docker Hub API 确认 PostgreSQL、RabbitMQ、MinIO、Go、Alpine、Node、Nginx 七个标签存在，并将相应多架构摘要固定到 Compose/Dockerfile |
| Compose 完整运行 | 通过 | Docker Engine 29.6.2、Docker Desktop 4.83.0、Compose 5.3.1；完成镜像构建和全栈启动，PostgreSQL、RabbitMQ、MinIO、四个领域服务、网关及 Vue 前端全部 Healthy；HTTP 与容器内健康检查通过；详见 `compose-smoke.log` |

## 已知剩余风险

这些项目不属于阶段一业务实现，但必须在后续月份按计划消除：

- Token 撤销集合仍在单进程内存中，重启后丢失，也不支持多实例共享；第 3 月由 IAM 的持久化 Refresh Token、轮换和重复使用检测替代。
- 冻结的旧前端仍有内联事件与内联样式，因此旧 CSP 暂时保留 `unsafe-inline`；新 Vue 3 前端已禁止内联脚本，旧系统在正式切换后下线。
- 旧导入虽然有 50 MiB 上限和统一校验，仍是同步处理、逐行提交和部分成功；流式预检、批次事务、幂等、重试和错误报告属于第 5 月。
- 旧 SQLite 没有版本化迁移、完整审计写入和自动备份恢复；历史数据迁移在第 6 月推进，审计和备份分别在第 8、11 月推进。
- 旧系统只有粗粒度 `admin/reporter`，缺少审核员、审计员和部门级策略；第 3 月 IAM 建设时扩展并形成新平台权限矩阵。
- JWT 仍采用旧系统的 HS256；新平台第 3 月切换非对称签名和 15 分钟 Access Token。
- Vue 基线为便于尽早验证而全量引入 Element Plus，首屏压缩前 JavaScript 约 1 MiB；后续页面建设时采用按需组件和路由分包，第 10 月纳入性能门禁。
- 当前 Git 索引在本次改造前已呈现“历史文件暂存删除、工作区同名文件未跟踪”的异常状态。本次未执行暂存、重置或提交，避免覆盖既有 Git 状态；合并阶段一前应由项目所有者确认基线。

## 阶段一最终验收

2026-07-22 在已安装 Docker Desktop 的 Windows 环境执行：

```bash
sh scripts/compose-smoke.sh
```

Windows 可执行：

```powershell
.\scripts\compose-smoke.ps1
```

执行结果为 `Compose integration smoke test passed`。脚本证明 PostgreSQL、RabbitMQ、MinIO、四个领域服务、网关和前端均达到健康状态；网关和前端 HTTP 检查通过，日志未发现 panic、fatal、error 或 unhealthy。脚本随后移除所有容器和网络，本次明确新建的三个测试数据卷也在核对 Compose 标签与创建时间后删除，保留 `compose-smoke.log` 作为证据。

## 历史阻塞与恢复记录

截至 2026-07-21，同一阻塞条件已连续确认三次：

1. 首次验收发现当前 Windows 环境没有 Docker、Podman 或可用容器守护进程，无法执行全栈 Compose 冒烟。
2. 第二次使用按官方 SHA-256 校验的 Docker Compose v5.1.4 独立程序完成 `compose config --quiet`，并确认全部镜像标签与摘要存在；但独立解析器不能替代容器守护进程，仍无法启动服务。
3. 第三次重新检查确认：容器运行时命令数量为 0、运行中的容器服务数量为 0、WSL 不可用；阻塞条件没有变化。

2026-07-22 用户安装并启动 Docker Desktop 后阻塞解除。恢复验收过程如下：

- 确认 Docker Engine 29.6.2 与 Compose 5.3.1 可用。
- 首次并行构建因 Docker Hub 鉴权端点 IPv6 连接超时失败；基础设施镜像已成功下载，且没有遗留容器。
- 逐个按固定摘要拉取 Go、Alpine、Node、Nginx 基础镜像后重试。
- 第二次完整构建、健康等待、HTTP 检查和服务内健康检查全部通过。

阶段一阻塞状态已关闭。按本目标约束在阶段一完成后停止，阶段二仍未开始。

## 阶段二进入条件

1. Compose 完整运行验收已通过。
2. 确认并修复当前 Git 索引基线，保留阶段一文件和历史资料。
3. 将旧系统 `JWT_SECRET`、`ADMIN_PASSWORD` 以及新平台基础设施密码纳入部署密钥管理。
4. 根据 `权限矩阵.md` 完成业务方确认，特别确认 reporter 的数据范围为“本人”。
