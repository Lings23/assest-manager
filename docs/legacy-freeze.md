# 旧系统功能冻结规则

自阶段一完成之日起，根目录旧 Go/SQLite 应用（`main.go`、`internal/`、`web/`）进入功能冻结。

允许的修改仅包括：

- 阻断上线或迁移的 P0/P1 安全缺陷；
- 数据损坏、无法登录或无法导出迁移数据等阻断性缺陷；
- 为 SQLite→PostgreSQL 迁移提供只读兼容能力。

禁止在旧系统新增资产类型、审批流程、报表、角色模型或界面功能。确需例外时，PR 必须说明风险、最小改动范围、回归证据和移除日期，并由技术负责人批准。所有新能力进入 `platform/`、`apps/web/` 和相应新架构目录。
