# 旧系统权限矩阵（第 1 月基线）

## 1. 适用范围

本矩阵约束重构切换前旧系统的 `admin` 和 `reporter` 两类角色。后端授权是安全边界；前端隐藏按钮仅用于避免误操作，不能替代后端校验。

数据范围定义：

- `全部`：可访问所有未删除记录。
- `本人`：仅可访问 `created_by` 等于当前用户 ID 的记录。
- `无`：后端返回 `403 Forbidden`，且前端不展示入口。

## 2. 资产与统计权限

| 能力 | admin | reporter | 后端约束 |
|---|---|---|---|
| 资产列表、搜索 | 全部 | 本人 | 所有列表查询统一附加软删除和数据范围条件 |
| 资产详情 | 全部 | 本人 | 单条查询与列表使用相同数据范围 |
| 新建资产 | 允许 | 允许 | `created_by` 只能由服务端写入当前用户 ID |
| 更新资产 | 全部 | 本人 | 更新条件原子包含数据范围，禁止客户端写系统字段 |
| 软删除资产 | 允许 | 无 | 路由和处理器均以管理员权限为准，前端仅管理员显示按钮 |
| 查看或恢复已删除资产 | 无 | 无 | 旧系统当前不暴露该入口，统一在后续资产服务实现 |
| 导入 | 允许 | 允许 | 导入记录归属当前用户，逐行复用与创建接口相同的校验 |
| 导出 | 全部 | 本人 | 导出与列表使用相同数据范围 |
| 统计 | 全部 | 本人 | 汇总、分组和高风险统计使用相同数据范围和编码定义 |
| 软件累计计算 | 全部 | 本人 | 计算范围必须附加角色数据范围条件 |

## 3. 认证权限

| 能力 | 未登录用户 | 已登录用户 | 约束 |
|---|---|---|---|
| 登录 | 允许 | 允许 | 按 IP 与用户名组合限流，失败信息不暴露用户是否存在 |
| 查看当前用户 | 无 | 允许 | 必须携带有效且未撤销的 Access Token |
| 修改本人密码 | 无 | 允许 | 校验旧密码；新密码至少 12 位；成功后撤销当前 Token |
| 注销 | 无 | 允许 | 当前 Token 立即加入撤销集合 |
| 管理其他用户 | 无 | 无 | 旧系统未提供该能力，统一在后续 IAM 服务实现 |

## 4. 一致性验收

- 同一角色在列表、详情、统计、导出和累计计算中的数据范围必须一致。
- `id`、`created_by`、`created_at`、`updated_at`、`is_deleted` 和未知字段一律不得由请求体写入。
- reporter 更新非本人记录和删除任意记录均应失败；不能只依赖前端按钮状态。
- 权限策略调整时必须先更新本矩阵和自动化测试，再修改实现。
